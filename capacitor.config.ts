import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dallastjames.testing.sveltetest',
  appName: 'svelte-test',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
